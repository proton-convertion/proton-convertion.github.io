# В данном форке

* AWG 3.0 включён по умолчанию,
* если I2 не задан, то задаётся таким же как I1, дублирование требуется, например, чтобы работало в сети Wi-Fi в метро MT_FREE,
* изменён список SNI.
